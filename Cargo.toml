[package]
name = "trade-bot"
version = "0.1.0"
edition = "2024"

[dependencies]
kraken-async-rs = "0.13.0"
tokio = {version="1.47.2", features=["full"]}
tokio-stream = {version="0.1.17", features=["full"]}
tracing = {version="0.1.41", features=["log"]}
